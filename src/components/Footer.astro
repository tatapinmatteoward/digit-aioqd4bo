---
const currentYear = new Date().getFullYear();
const socialLinks = [
  { icon: 'fab fa-twitter', url: '#' },
  { icon: 'fab fa-facebook', url: '#' },
  { icon: 'fab fa-linkedin', url: '#' },
  { icon: 'fab fa-instagram', url: '#' }
];
---

<footer class="bg-[#001f4d] text-white py-12">
  <div class="container mx-auto px-4">
    <div class="flex flex-col md:flex-row justify-between items-center">
      <div class="mb-6 md:mb-0">
        <p class="text-lg font-semibold">Digital Rights Law Group</p>
        <p class="text-[#ff6b6b] mt-2">&copy; {currentYear} Digital Rights Law Group. All rights reserved.</p>
      </div>
      
      <div class="flex space-x-6">
        {socialLinks.map((link) => (
          <a 
            href={link.url} 
            class="text-white hover:text-[#ff6b6b] transition-colors duration-300"
            aria-label="Social media link"
          >
            <i class={link.icon}></i>
          </a>
        ))}
      </div>
    </div>
    
    <div class="border-t border-[#ff6b6b] mt-8 pt-8 text-center text-sm text-gray-300">
      <p>Protecting digital rights through innovative legal solutions</p>
    </div>
  </div>
</footer>